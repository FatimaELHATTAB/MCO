Hello,

The work currently being carried out on the tactical solution is aligned with short-term delivery needs, as this solution is already in production and supports several providers (CDP, Sustainalytics, MSCI and Bloomberg).

As planned, the developments are currently progressing on the tactical solution, with the following scope:

* the integration of a new provider (Ecovadis), for which a solution was presented to end users in December,
* the internalization of the fuzzy matching logic (replacing the external API that is being phased out).

Fuzzy matching currently applies to CDP, Sustainalytics and MSCI and represents approximately 2,300 third parties (~3% of the total matched population).

The roadmap remains on track over a three-week period, with a first version expected early next week.

Regarding testing, the plan is the following:

* focus on the fuzzy matching logic, as this is where the main changes are implemented,
* validate the results on the existing providers using fuzzy (CDP, Sustainalytics, MSCI),
* include Ecovadis data in the validation scope, in line with what was presented to end users,
* perform broader non-regression checks across all providers to ensure overall consistency.

While the main changes are concentrated on fuzzy (with a limited scope of ~3%), the full solution will still be covered to avoid any side effects.

In addition, this work directly contributes to the target solution:

* fuzzy internalization is a required component for the target setup,
* the tests executed now will be reused for future provider integrations, ensuring continuity and saving time.

The current plan remains aligned with the initial timeline and scope.

Best regards,
[Your Name]
