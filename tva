Hello,

The work currently being carried out on the tactical solution is aligned with short-term delivery needs, as this solution is already in production and supports existing providers.

The current scope includes:

the integration of a new provider (Ecovadis),
the internalization of the fuzzy matching logic (replacing the external API that is being phased out).

This roadmap has been planned over a three-week period and is progressing as expected. A first version will be available by early next week, allowing us to start non-regression testing shortly after.

From a risk and effort perspective:

fuzzy matching currently represents approximately 2,300 third parties (~3% of the total scope),
this limited perimeter significantly reduces the risk of unexpected impacts during non-regression testing,
based on this scope, the testing phase remains controlled and should not lead to timeline overruns.

In addition, the work performed on the tactical solution directly contributes to the target solution:

the fuzzy matching internalization is a required building block for the target setup,
non-regression tests executed now will be reused when integrating providers into the target solution, ensuring consistency and saving time in upcoming phases.

Overall, this approach allows us to secure current deliveries while efficiently preparing the target solution, with a controlled and realistic timeline.

Please let me know if you would like to discuss this further.

Best regards,
